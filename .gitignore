# Auto-generated at backend startup (contains machine-specific WSL paths)
noahbot_mcp_config.json

# Runtime state (changes frequently, machine-specific)
conflict_cache.json
sms_drafts.json

# Per-machine settings (contains credentials — never commit)
config.local.json

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Backup files
*.bak

# Log files
*.log

# Local environment / credentials (for future use)
.env
config.local.json

# macOS
.DS_Store
